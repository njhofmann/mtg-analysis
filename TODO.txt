-Figure out why psycopg2 doesn't close connections
-Implement scrappers: mtgtop8, financal data?
    -mtgtop8: parse tournament page, store related urls, traverse list
    -figure out what PageSubmit function is doing, recreate functionality in code and traverse
-Add specific versions of each dependency to readme and requirements
